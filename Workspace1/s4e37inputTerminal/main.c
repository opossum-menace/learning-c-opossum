#include <stdio.h>

int main()
{
	
		char str[100];
		int i=0;
		double x=0;
		
		printf("enter a value sir: ");
		scanf("%s %d %lf", str, &i, &x);
		printf("\nYou entered: %s %d %lf", str, i, x);
		
		return 0;
	
	
}

